---
title: 英文模式
order: 1
description: 进入英文模式的方式；英文模式为纯直通，不组句、不出候选、不转全角。
---

## 进入英文模式

- **macOS**：`Caps Lock` 亮起为英文模式，熄灭回到中文。
- **Windows**：单击 `Shift`（按下即松开，不与其他键组合）在中 / 英之间切换，任务栏右下同步显示「中」或「英」。
  反向同样有效：点击任务栏的「中 / 英」图标，青简随之切换（悬浮状态条一并更新）。
  `Caps Lock` 在 Windows 上只控制大小写：亮起时无论中英模式均直接输出大写英文字母（与微软拼音一致）；`Shift` + 字母同样输出大写。

## 悬浮状态条（Windows）

任务栏的「中 / 英」不易察觉时，可另开一条**桌面上的悬浮状态条**：「设置 → 候选窗口」打开「悬浮状态条」。它有三格：

| 格 | 显示 | 点击 |
| --- | --- | --- |
| `中` / `注` / `英` | 当前模式（开启大千注音时显示「注」，双拼时显示为「中 · 小鹤双拼」） | 切换中 / 英，与单击 `Shift` 相同 |
| `，。` / `,.` | 当前模式下标点是否转为全角。中英各记一份，缺省中文全角、英文半角，切换模式时随之切换 | 切换当前模式的全角 / 半角 |
| ⚙ | | 打开设置 |

按住拖动可移到屏幕任意位置，位置会被记住；外观随系统的深浅色与缩放。
它只在当前输入法为青简时显示：切换到微软拼音等其他输入法时收起，切回后再次出现；关闭某个应用不影响它。它与任务栏图标并存。

## 行为

英文模式为**纯直通**：按键原样交给应用，与未使用输入法时一致。

- 不组句、不出候选窗口、不补全、不拼写纠正；
- 标点保持半角（`?` 就是 `?`，不会变成 `？`）；
- 字母大小写由系统 / 应用按 `Shift` 与 `Caps Lock` 自行处理。

切换到英文模式时，若中文模式下还在组句中的拼音会先落定上屏，之后的按键即进入直通。

## 与中文模式的关系

中文模式下整段输入为英文词时同样直接给出英文候选（`hello` 先英文再拼音，勾选「中文候选排在英文词前面」后英文排第二），拼音不成立时还会补全前缀（`compa` → company）。
无需切换到英文模式，见 [快捷输入](shortcuts.md)。
